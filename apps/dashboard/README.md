# Operations dashboard

The fleet-management surface of the [NextGen Vending Platform](../../README.md). It covers onboarding, machine status and detail views, inventory, notifications, live monitoring, and operator settings.

Copy `.env.example` to `.env.local`, then run `npm ci` and `npm run dev`. See the repository root for architecture, verification, and portfolio-sanitization notes.
